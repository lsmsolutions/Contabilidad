{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Silveira Accounting",
      "type": "node-terminal",
      "request": "launch",
      "command": "${workspaceFolder}\\run.bat",
      "cwd": "${workspaceFolder}"
    }
  ]
}
